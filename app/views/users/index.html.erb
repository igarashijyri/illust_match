<h1>All users</h1>
 
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <%= image_tag("user_#{@user_id}.jpg", width:300, height:250)  %>
    </li>
  <% end %>
</ul>

<%= link_to 'LOGOUT', destroy_user_session_path, method: :delete %>
